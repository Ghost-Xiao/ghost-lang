# 内置模块说明

Ghost Language 提供了以下内置模块，用于常见的功能。

## 模块导入方式

使用 `import` 语句导入内置模块（注意模块名是字符串）：

```ghost
import "math";
import "fmt";
import "io";
```

导入的模块可以通过成员访问表达式访问其成员：
```ghost
import "math";
math.PI;
math.sqrt(4);
```

---

## fmt 模块

格式化输出模块，提供字符串格式化和打印功能。

### 导入方式
```ghost
import "fmt";
```

### sprintf 函数

#### 功能描述
根据格式化字符串格式化参数，返回格式化后的字符串。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| format | 字符串 | 无 | 格式化字符串 |
| 参数1, 参数2, ... | 任意类型 | 无 | 格式化参数，可以是多个 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 字符串 | 格式化后的字符串 |

#### 使用示例
```ghost
import "fmt";
fmt.sprintf("Hello, %s!", "World"); // 返回: "Hello, World!"
fmt.sprintf("Number: %d", 42); // 返回: "Number: 42"
fmt.sprintf("Float: %f", 3.14); // 返回: "Float: 3.140000"
fmt.sprintf("Bool: %t", true); // 返回: "Bool: true"
fmt.sprintf("Binary: %b", 42); // 返回: "Binary: 101010"
fmt.sprintf("Hex: %x", 42); // 返回: "Hex: 2a"
```

#### 格式化说明符
| 说明符 | 说明 |
|--------|------|
| %v | 默认格式，输出值的字符串表示 |
| %d | 整数格式 |
| %f | 浮点数格式 |
| %s | 字符串格式 |
| %t | 布尔值格式 |
| %b | 二进制格式 |
| %x | 十六进制格式 |
| %% | 输出百分号 |

---

### printf 函数

#### 功能描述
根据格式化字符串格式化参数，并打印到标准输出。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| format | 字符串 | 无 | 格式化字符串 |
| 参数1, 参数2, ... | 任意类型 | 无 | 格式化参数，可以是多个 |

#### 返回值
| 类型 | 说明 |
|------|------|
| null | 函数执行后返回空值 |

#### 使用示例
```ghost
import "fmt";
fmt.printf("Hello, %s!\n", "World"); // 输出: Hello, World!
fmt.printf("Number: %d\n", 42); // 输出: Number: 42
```

---

## math 模块

数学模块，提供常用的数学常量和函数。

### 导入方式
```ghost
import "math";
```

### 数学常量

| 常量 | 值 | 说明 |
|------|-----|------|
| PI | 3.141592653589793 | 圆周率 |
| E | 2.718281828459045 | 自然对数的底 |
| TAU | 6.283185307179586 | 圆周率的两倍，即圆周长 |

---

### abs 函数

#### 功能描述
返回一个数的绝对值。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| x | 整数或浮点数 | 无 | 要计算绝对值的数 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数或浮点数 | 绝对值，保持原类型 |

#### 使用示例
```ghost
import "math";
math.abs(-42); // 返回: 42
math.abs(-3.14); // 返回: 3.14
```

---

### sqrt 函数

#### 功能描述
返回一个数的平方根。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| x | 整数或浮点数 | 无 | 要计算平方根的数 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 浮点数 | 平方根 |

#### 使用示例
```ghost
import "math";
math.sqrt(4); // 返回: 2.0
math.sqrt(2); // 返回: 1.4142135623730951
```

#### 异常处理
如果参数小于 0，函数会抛出操作错误：
```ghost
math.sqrt(-1); // 抛出操作错误: sqrt of negative number.
```

---

### sin, cos, tan 函数

#### 功能描述
三角函数，返回角度的正弦、余弦、正切值。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| x | 整数或浮点数 | 无 | 角度（弧度） |

#### 返回值
| 类型 | 说明 |
|------|------|
| 浮点数 | 三角函数值 |

#### 使用示例
```ghost
import "math";
math.sin(math.PI / 2); // 返回: 1.0
math.cos(math.PI); // 返回: -1.0
math.tan(math.PI / 4); // 返回: 1.0
```

---

### asin, acos, atan 函数

#### 功能描述
反三角函数，返回正弦、余弦、正切的反函数值。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| x | 整数或浮点数 | 无 | 三角函数值 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 浮点数 | 反三角函数值（弧度） |

#### 使用示例
```ghost
import "math";
math.asin(1); // 返回: 1.5707963267948966
math.acos(-1); // 返回: 3.141592653589793
math.atan(1); // 返回: 0.7853981633974483
```

---

### log 函数

#### 功能描述
计算以指定底数的对数。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| a | 整数或浮点数 | 无 | 底数 |
| x | 整数或浮点数 | 无 | 真数 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 浮点数 | 对数值 |

#### 使用示例
```ghost
import "math";
math.log(2, 8); // 返回: 3.0
math.log(10, 100); // 返回: 2.0
```

#### 异常处理
- 底数必须大于 0 且不等于 1
- 真数必须大于 0

---

### lg 函数

#### 功能描述
计算以 10 为底的对数。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| x | 整数或浮点数 | 无 | 真数 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 浮点数 | 对数值 |

#### 使用示例
```ghost
import "math";
math.lg(100); // 返回: 2.0
math.lg(1000); // 返回: 3.0
```

---

### ln 函数

#### 功能描述
计算以自然对数 e 为底的对数。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| x | 整数或浮点数 | 无 | 真数 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 浮点数 | 对数值 |

#### 使用示例
```ghost
import "math";
math.ln(math.E); // 返回: 1.0
math.ln(1); // 返回: 0.0
```

---

### floor 函数

#### 功能描述
向下取整，返回小于或等于给定数的最大整数或保留指定小数位数。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| x | 整数或浮点数 | 无 | 要取整的数 |
| decimalPlaces | 整数 | 0 | 保留的小数位数（可选） |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数或浮点数 | 如果 decimalPlaces 为 0，返回整数；否则返回浮点数 |

#### 使用示例
```ghost
import "math";
math.floor(3.14); // 返回: 3
math.floor(-3.14); // 返回: -4
math.floor(3.14159, 2); // 返回: 3.14
math.floor(3.14159, 3); // 返回: 3.141
```

#### 注意事项
- 当 decimalPlaces 为 0 或未提供时，返回整数
- 当 decimalPlaces 大于 0 时，返回浮点数，保留指定小数位数

---

### ceil 函数

#### 功能描述
向上取整，返回大于或等于给定数的最小整数或保留指定小数位数。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| x | 整数或浮点数 | 无 | 要取整的数 |
| decimalPlaces | 整数 | 0 | 保留的小数位数（可选） |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数或浮点数 | 如果 decimalPlaces 为 0，返回整数；否则返回浮点数 |

#### 使用示例
```ghost
import "math";
math.ceil(3.14); // 返回: 4
math.ceil(-3.14); // 返回: -3
math.ceil(3.14159, 2); // 返回: 3.15
math.ceil(3.14159, 3); // 返回: 3.142
```

#### 注意事项
- 当 decimalPlaces 为 0 或未提供时，返回整数
- 当 decimalPlaces 大于 0 时，返回浮点数，保留指定小数位数

---

### round 函数

#### 功能描述
四舍五入，返回最接近给定数的整数或保留指定小数位数。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| x | 整数或浮点数 | 无 | 要取整的数 |
| decimalPlaces | 整数 | 0 | 保留的小数位数（可选） |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数或浮点数 | 如果 decimalPlaces 为 0，返回整数；否则返回浮点数 |

#### 使用示例
```ghost
import "math";
math.round(3.14); // 返回: 3
math.round(3.5); // 返回: 4
math.round(3.14159, 2); // 返回: 3.14
math.round(3.14159, 3); // 返回: 3.142
math.round(3.6789, 1); // 返回: 3.7
```

#### 注意事项
- 当 decimalPlaces 为 0 或未提供时，返回整数
- 当 decimalPlaces 大于 0 时，返回浮点数，保留指定小数位数

---

### min 函数

#### 功能描述
返回一组数中的最小值。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 参数1, 参数2, ... | 整数或浮点数 | 无 | 要比较的数，可以是多个 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数或浮点数 | 最小值 |

#### 使用示例
```ghost
import "math";
math.min(5, 3, 7, 1); // 返回: 1
math.min(2.5, 1.8, 3.2); // 返回: 1.8

// 使用解包
nums = [5, 3, 7, 1];
math.min(...nums); // 返回: 1
```

---

### max 函数

#### 功能描述
返回一组数中的最大值。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 参数1, 参数2, ... | 整数或浮点数 | 无 | 要比较的数，可以是多个 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数或浮点数 | 最大值 |

#### 使用示例
```ghost
import "math";
math.max(5, 3, 7, 1); // 返回: 7
math.max(2.5, 1.8, 3.2); // 返回: 3.2

// 使用解包
nums = [5, 3, 7, 1];
math.max(...nums); // 返回: 7
```

---

### sum 函数

#### 功能描述
计算一组数的总和。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 参数1, 参数2, ... | 整数或浮点数 | 无 | 要相加的数，可以是多个 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数或浮点数 | 总和 |

#### 使用示例
```ghost
import "math";
math.sum(1, 2, 3, 4, 5); // 返回: 15
math.sum(1.5, 2.5, 3.5); // 返回: 7.5

// 使用解包
nums = [1, 2, 3, 4, 5];
math.sum(...nums); // 返回: 15
```

---

### product 函数

#### 功能描述
计算一组数的乘积。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 参数1, 参数2, ... | 整数或浮点数 | 无 | 要相乘的数，可以是多个 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数或浮点数 | 乘积 |

#### 使用示例
```ghost
import "math";
math.product(1, 2, 3, 4, 5); // 返回: 120
math.product(2, 3, 4); // 返回: 24

// 使用解包
nums = [1, 2, 3, 4, 5];
math.product(...nums); // 返回: 120
```

---

### mean 函数

#### 功能描述
计算一组数的平均值。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 参数1, 参数2, ... | 整数或浮点数 | 无 | 要计算平均值的数，可以是多个 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 浮点数 | 平均值 |

#### 使用示例
```ghost
import "math";
math.mean(1, 2, 3, 4, 5); // 返回: 3.0
math.mean(2, 4, 6); // 返回: 4.0

// 使用解包
nums = [1, 2, 3, 4, 5];
math.mean(...nums); // 返回: 3.0
```

---

### median 函数

#### 功能描述
计算一组数的中位数。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 参数1, 参数2, ... | 整数或浮点数 | 无 | 要计算中位数的数，可以是多个 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 浮点数 | 中位数 |

#### 使用示例
```ghost
import "math";
math.median(1, 3, 5, 7, 9); // 返回: 5.0
math.median(1, 3, 5, 7); // 返回: 4.0

// 使用解包
nums = [1, 3, 5, 7, 9];
math.median(...nums); // 返回: 5.0
```

---

### variance 函数

#### 功能描述
计算一组数的方差。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 参数1, 参数2, ... | 整数或浮点数 | 无 | 要计算方差的数，可以是多个 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 浮点数 | 方差 |

#### 使用示例
```ghost
import "math";
math.variance(1, 2, 3, 4, 5); // 返回: 2.0

// 使用解包
nums = [1, 2, 3, 4, 5];
math.variance(...nums); // 返回: 2.0
```

---

### stdDev 函数

#### 功能描述
计算一组数的标准差。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| 参数1, 参数2, ... | 整数或浮点数 | 无 | 要计算标准差的数，可以是多个 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 浮点数 | 标准差 |

#### 使用示例
```ghost
import "math";
math.stdDev(1, 2, 3, 4, 5); // 返回: 1.4142135623730951

// 使用解包
nums = [1, 2, 3, 4, 5];
math.stdDev(...nums); // 返回: 1.4142135623730951
```

---

### rand 函数

#### 功能描述
生成一个 0 到 1 之间的随机浮点数。

#### 参数列表
无参数

#### 返回值
| 类型 | 说明 |
|------|------|
| 浮点数 | 随机浮点数（0 &lt;= x &lt; 1） |

#### 使用示例
```ghost
import "math";
math.rand(); // 返回: 0.54321... (随机值)
```

---

### randInt 函数

#### 功能描述
生成指定范围内的随机整数。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| min | 整数或浮点数 | 无 | 最小值（包含） |
| max | 整数或浮点数 | 无 | 最大值（包含） |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数 | 随机整数（min &lt;= x &lt;= max） |

#### 使用示例
```ghost
import "math";
math.randInt(1, 10); // 返回: 5 (1 到 10 之间的随机整数)

// 使用解包
range = [1, 10];
math.randInt(...range); // 返回: 7 (1 到 10 之间的随机整数)
```

---

### factorial 函数

#### 功能描述
计算一个数的阶乘（n!），即 1×2×3×...×n。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| n | 整数或浮点数 | 无 | 要计算阶乘的数，必须是非负整数且不大于 20 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数 | 阶乘结果 |

#### 使用示例
```ghost
import "math";
math.factorial(0);  // 返回: 1
math.factorial(1);  // 返回: 1
math.factorial(5);  // 返回: 120
math.factorial(10); // 返回: 3628800
```

#### 异常处理
- 参数必须是非负整数
- 参数不能大于 20（防止整数溢出）

```ghost
math.factorial(-5); // 抛出操作错误: factorial of negative number.
math.factorial(21); // 抛出操作错误: factorial number too large.
```

---

### comb 函数

#### 功能描述
计算组合数 C(n, k)，即从 n 个元素中选取 k 个元素的组合数。

公式：C(n, k) = n! / (k! × (n-k)!)

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| n | 整数或浮点数 | 无 | 总元素数，必须是非负整数 |
| k | 整数或浮点数 | 无 | 选取的元素数，必须是非负整数 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数 | 组合数结果 |

#### 使用示例
```ghost
import "math";
math.comb(5, 0);  // 返回: 1
math.comb(5, 1);  // 返回: 5
math.comb(5, 2);  // 返回: 10
math.comb(5, 3);  // 返回: 10
math.comb(5, 5);  // 返回: 1
math.comb(10, 4); // 返回: 210
```

#### 特性
- 如果 k > n，返回 0
- 如果 k == 0 或 k == n，返回 1
- 自动优化计算：C(n, k) = C(n, n-k)，减少计算量

#### 异常处理
- 参数必须是非负整数

```ghost
math.comb(-5, 2); // 抛出操作错误: comb arguments must be non-negative.
math.comb(5, -2); // 抛出操作错误: comb arguments must be non-negative.
```

---

### perm 函数

#### 功能描述
计算排列数 P(n, k)，即从 n 个元素中选取 k 个元素进行排列的方式数。

公式：P(n, k) = n! / (n-k)!

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| n | 整数或浮点数 | 无 | 总元素数，必须是非负整数 |
| k | 整数或浮点数 | 无 | 选取的元素数，必须是非负整数 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数 | 排列数结果 |

#### 使用示例
```ghost
import "math";
math.perm(5, 0);  // 返回: 1
math.perm(5, 1);  // 返回: 5
math.perm(5, 2);  // 返回: 20
math.perm(5, 5);  // 返回: 120
math.perm(10, 3); // 返回: 720
```

#### 特性
- 如果 k > n，返回 0
- 如果 k == 0，返回 1

#### 异常处理
- 参数必须是非负整数

```ghost
math.perm(-5, 2); // 抛出操作错误: perm arguments must be non-negative.
math.perm(5, -2); // 抛出操作错误: perm arguments must be non-negative.
```

---

## io 模块

文件输入输出模块，提供文件操作功能。

### 导入方式
```ghost
import "io";
```

### read 函数

#### 功能描述
读取整个文件的内容并返回。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| path | 字符串 | 无 | 文件路径 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 字符串 | 文件内容 |

#### 使用示例
```ghost
import "io";
content = io.read("file.txt"); // 读取文件内容
```

---

### write 函数

#### 功能描述
将内容写入文件，覆盖原有内容。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| path | 字符串 | 无 | 文件路径 |
| content | 字符串 | 无 | 要写入的内容 |

#### 返回值
| 类型 | 说明 |
|------|------|
| null | 函数执行后返回空值 |

#### 使用示例
```ghost
import "io";
io.write("file.txt", "Hello, World!"); // 写入文件
```

---

### append 函数

#### 功能描述
将内容追加到文件末尾。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| path | 字符串 | 无 | 文件路径 |
| content | 字符串 | 无 | 要追加的内容 |

#### 返回值
| 类型 | 说明 |
|------|------|
| null | 函数执行后返回空值 |

#### 使用示例
```ghost
import "io";
io.append("file.txt", "More content!"); // 追加内容到文件
```

---

### readLines 函数

#### 功能描述
读取文件内容并按行分割，返回字符串列表。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| path | 字符串 | 无 | 文件路径 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 列表 | 字符串列表，每个元素是文件的一行 |

#### 使用示例
```ghost
import "io";
lines = io.readLines("file.txt"); // 读取文件并返回行列表
```

---

### mkdir 函数

#### 功能描述
创建目录（包括所有必要的父目录）。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| path | 字符串 | 无 | 目录路径 |

#### 返回值
| 类型 | 说明 |
|------|------|
| null | 函数执行后返回空值 |

#### 使用示例
```ghost
import "io";
io.mkdir("mydir"); // 创建目录
```

---

### listDir 函数

#### 功能描述
列出指定目录下的所有文件和子目录。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| path | 字符串 | 无 | 目录路径 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 列表 | 字符串列表，每个元素是文件名或目录名 |

#### 使用示例
```ghost
import "io";
files = io.listDir("."); // 列出当前目录的内容
```

---

### exists 函数

#### 功能描述
检查指定路径的文件或目录是否存在。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| path | 字符串 | 无 | 要检查的路径 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 布尔值 | 如果路径存在返回 true，否则返回 false |

#### 使用示例
```ghost
import "io";
io.exists("file.txt"); // 检查文件是否存在
```

---

### isFile 函数

#### 功能描述
检查指定路径是否是文件。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| path | 字符串 | 无 | 要检查的路径 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 布尔值 | 如果是文件返回 true，否则返回 false |

#### 使用示例
```ghost
import "io";
io.isFile("file.txt"); // 检查是否是文件
```

---

### isDir 函数

#### 功能描述
检查指定路径是否是目录。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| path | 字符串 | 无 | 要检查的路径 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 布尔值 | 如果是目录返回 true，否则返回 false |

#### 使用示例
```ghost
import "io";
io.isDir("mydir"); // 检查是否是目录
```

---

### fileSize 函数

#### 功能描述
获取文件的大小（字节数）。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| path | 字符串 | 无 | 文件路径 |

#### 返回值
| 类型 | 说明 |
|------|------|
| 整数 | 文件大小（字节） |

#### 使用示例
```ghost
import "io";
size = io.fileSize("file.txt"); // 获取文件大小
```

---

### copy 函数

#### 功能描述
复制文件。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| src | 字符串 | 无 | 源文件路径 |
| dst | 字符串 | 无 | 目标文件路径 |

#### 返回值
| 类型 | 说明 |
|------|------|
| null | 函数执行后返回空值 |

#### 使用示例
```ghost
import "io";
io.copy("src.txt", "dst.txt"); // 复制文件
```

---

### move 函数

#### 功能描述
移动或重命名文件。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| src | 字符串 | 无 | 源文件路径 |
| dst | 字符串 | 无 | 目标文件路径 |

#### 返回值
| 类型 | 说明 |
|------|------|
| null | 函数执行后返回空值 |

#### 使用示例
```ghost
import "io";
io.move("old.txt", "new.txt"); // 重命名文件
```

---

### remove 函数

#### 功能描述
删除文件或目录（包括目录下的所有内容）。

#### 参数列表
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| path | 字符串 | 无 | 要删除的路径 |

#### 返回值
| 类型 | 说明 |
|------|------|
| null | 函数执行后返回空值 |

#### 使用示例
```ghost
import "io";
io.remove("file.txt"); // 删除文件
io.remove("mydir"); // 删除目录
```

---

## time 模块

时间模块，提供时间处理和日期操作功能。

### 导入方式
```ghost
import "time";
```

### Time 类

时间类，表示一个具体的时间点。

#### 构造函数

Time 类支持多种构造方式：

| 构造方式 | 描述 | 示例 |
|----------|------|------|
| 无参数 | 创建当前时间 | `time.Time()` |
| 时间戳（纳秒） | 从 Unix 纪元开始的纳秒数 | `time.Time(1715241600000000000)` |
| 格式字符串和日期字符串 | 解析日期时间 | `time.Time("yyyy-MM-dd HH:mm:ss", "2024-05-09 12:30:45")` |
| 年、月、日 | 创建指定日期 | `time.Time(2024, 5, 9)` |
| 年、月、日、时、分、秒 | 创建完整日期时间 | `time.Time(2024, 5, 9, 12, 30, 45)` |

---

#### Time 类方法

| 方法 | 描述 | 参数 | 返回值 | 示例 |
|------|------|------|--------|------|
| timestamp | 获取时间戳（纳秒） | 无 | 整数（纳秒） | `t.timestamp()` |
| setZone | 设置时区偏移（小时） | offset：时区偏移（小时） | 时区偏移 | `t.setZone(8)` |
| nanosecond | 获取纳秒部分（0-999999999） | 无 | 整数 | `t.nanosecond()` |
| microsecond | 获取微秒部分（0-999999） | 无 | 整数 | `t.microsecond()` |
| millisecond | 获取毫秒部分（0-999） | 无 | 整数 | `t.millisecond()` |
| year | 获取年份 | 无 | 整数 | `t.year()` |
| month | 获取月份（1-12） | 无 | 整数 | `t.month()` |
| day | 获取日期（1-31） | 无 | 整数 | `t.day()` |
| hour | 获取小时（0-23） | 无 | 整数 | `t.hour()` |
| minute | 获取分钟（0-59） | 无 | 整数 | `t.minute()` |
| second | 获取秒（0-59） | 无 | 整数 | `t.second()` |
| weekday | 获取星期几（0-6，0 表示周日） | 无 | 整数 | `t.weekday()` |
| yearday | 获取一年中的第几天（1-366） | 无 | 整数 | `t.yearday()` |
| format | 格式化时间为字符串 | layout：格式化布局（可选，默认 "yyyy-MM-dd HH:mm:ss"） | 字符串 | `t.format()`，`t.format("yyyy/MM/dd")` |

##### 格式化占位符

`format()` 方法使用 Java 风格的格式化字符串，支持以下占位符：

| 占位符 | 说明 | 示例 |
|--------|------|------|
| `y` | 年 | `yyyy` → 2024, `yy` → 24 |
| `M` | 月 | `MM` → 05, `M` → 5 |
| `d` | 日 | `dd` → 09, `d` → 9 |
| `H` | 小时（24小时制） | `HH` → 14, `H` → 14 |
| `h` | 小时（12小时制） | `hh` → 02, `h` → 2 |
| `m` | 分钟 | `mm` → 30, `m` → 30 |
| `s` | 秒 | `ss` → 45, `s` → 45 |
| `S` | 毫秒 | `SSS` → 123 |
| `a` | 上午/下午 | `a` → AM/PM |

常用格式示例：

```ghost
var t = time.Time(2024, 5, 9, 14, 30, 45);
t.format("yyyy-MM-dd");           // "2024-05-09"
t.format("yyyy/MM/dd HH:mm:ss");  // "2024/05/09 14:30:45"
t.format("MM/dd/yyyy");           // "05/09/2024"
t.format("HH:mm");                // "14:30"
```

##### 时间运算

Time 类支持加减运算：

```ghost
var t1 = time.Time();
var d = time.Duration(time.HOUR); // 1 小时

var t2 = t1 + d; // 时间加上时长
var t3 = t1 - d; // 时间减去时长
var diff = t2 - t1; // 两个时间相减得到时长
```

##### 时间比较

Time 类支持比较运算：

```ghost
t1 = time.Time(2024, 5, 1);
t2 = time.Time(2024, 6, 1);

t1 == t2; // 等于
t1 != t2; // 不等于
t1 < t2;  // 小于
t1 <= t2; // 小于等于
t1 > t2;  // 大于
t1 >= t2; // 大于等于
```

---

### Duration 类

时长类，表示一段时间间隔。

#### 构造函数

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| nanoseconds | 整数 | 无 | 纳秒数 |

```ghost
var d = time.Duration(time.MICROSECOND); // 1000 纳秒
var d = time.Duration(time.SECOND); // 1 秒
```

---

#### Duration 类方法

| 方法 | 描述 | 参数 | 返回值 | 示例 |
|------|------|------|--------|------|
| nanosecond | 获取纳秒数 | 无 | 整数 | `d.nanosecond()` |
| microsecond | 获取微秒数（浮点数） | 无 | 浮点数 | `d.microsecond()` |
| millisecond | 获取毫秒数（浮点数） | 无 | 浮点数 | `d.millisecond()` |
| second | 获取秒数（浮点数） | 无 | 浮点数 | `d.second()` |
| minute | 获取分钟数（浮点数） | 无 | 浮点数 | `d.minute()` |
| hour | 获取小时数（浮点数） | 无 | 浮点数 | `d.hour()` |
| day | 获取天数（浮点数） | 无 | 浮点数 | `d.day()` |

##### 时长运算

Duration 类支持基本运算：

```ghost
var d1 = time.Duration(time.HOUR); // 1 小时
var d2 = time.Duration(time.MINUTE * 30); // 30 分钟

var d3 = d1 + d2; // 加法
var d4 = d1 - d2; // 减法
var d5 = d1 * 2;  // 乘法
var d6 = d1 / 2;  // 除法
```

##### 时长比较

Duration 类支持比较运算：

```ghost
var d1 = time.Duration(time.HOUR); // 1 小时
var d2 = time.Duration(time.HOUR * 2); // 2 小时

d1 == d2; // 等于
d1 != d2; // 不等于
d1 < d2;  // 小于
d1 <= d2; // 小于等于
d1 > d2;  // 大于
d1 >= d2; // 大于等于
```

##### 时长取负

```ghost
var d = time.Duration(time.HOUR); // 1 小时
var neg_d = -d; // -1 小时
```

---

### sleep 函数

暂停当前执行指定的持续时间。

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| duration | Duration | 无 | 时长（Duration 实例） |

```ghost
import "time";

var d = time.Duration(time.SECOND); // 1 秒
time.sleep(d); // 暂停 1 秒
```

---

### 时间常量

time 模块提供了以下时间常量（单位：纳秒）：

| 常量 | 值 | 说明 |
|------|-----|------|
| `time.NANOSECOND` | 1 | 1 纳秒 |
| `time.MICROSECOND` | 1000 | 1 微秒 = 1000 纳秒 |
| `time.MILLISECOND` | 1000000 | 1 毫秒 = 1000000 纳秒 |
| `time.SECOND` | 1000000000 | 1 秒 = 1000000000 纳秒 |
| `time.MINUTE` | 60000000000 | 1 分钟 = 60 秒 |
| `time.HOUR` | 3600000000000 | 1 小时 = 60 分钟 |
| `time.DAY` | 86400000000000 | 1 天 = 24 小时 |

使用示例：
```ghost
import "time";

// 使用常量创建 Duration
var one_second = time.Duration(time.SECOND);
var one_hour = time.Duration(time.HOUR);

// 时间运算
var now = time.Time();
var one_hour_later = now + time.Duration(time.HOUR);
var one_minute_ago = now - time.Duration(time.MINUTE);

// 睡眠 3 秒
time.sleep(time.Duration(time.SECOND * 3));
```

---

### 使用示例

```ghost
import "time";

// 创建当前时间
var now = time.Time();
println(now.year());
println(now.month());
println(now.day());
println(now.format());

// 创建指定时间
var t = time.Time(2024, 5, 9, 12, 30, 45);
println(t.format("yyyy/MM/dd HH:mm:ss"));

// 从字符串解析时间
var t2 = time.Time("yyyy-MM-dd HH:mm:ss", "2024-06-01 08:00:00");
println(t2.year());

// 时间运算
var d = time.Duration(time.HOUR); // 1 小时
var later = now + d;
println(later.format());

// 时长计算
var diff = later - now;
println(diff.hour());

// 使用时间常量
var two_seconds = time.Duration(time.SECOND * 2);
var one_and_a_half_hours = time.Duration(time.HOUR + 30 * time.MINUTE);

// 睡眠
println("Sleeping for 2 seconds...");
time.sleep(two_seconds);
println("Done!");
```